# Requirements — DaVinci Resolve Wrapper API
# Python 3.10–3.12 recommended (NOT 3.13+)

fastapi==0.115.0
uvicorn[standard]==0.32.0
pydantic==2.9.2
pydantic-settings==2.6.0
watchdog==4.0.2      # Optional: watch folder support
httpx==0.27.2        # Optional: HTTP client for external systems
